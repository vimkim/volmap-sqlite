# Build and run the Inspector on your own frozen files.
mod user
# Generate and inspect small, frozen demonstration databases.
mod example

# Show every available command.
default:
    @just --list --list-submodules
